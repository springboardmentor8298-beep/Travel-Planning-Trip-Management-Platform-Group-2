import { useEffect, useState } from "react";
import { Link, useParams } from "react-router-dom";
import { ArrowLeft, MapPinned, CloudSun, Sparkles } from "lucide-react";
import api from "../services/api";

export default function DestinationDetails(){const {id}=useParams();const [item,setItem]=useState(null);const [error,setError]=useState("");useEffect(()=>{api.get(`/destinations/${id}`).then(r=>setItem(r.data)).catch(()=>setError("Destination details are unavailable."))},[id]);if(error)return <main className="workspace-page"><p className="form-error">{error}</p></main>;if(!item)return <main className="workspace-page">Loading destination...</main>;return <main className="workspace-page"><Link className="back-link" to="/destinations"><ArrowLeft size={16}/> Discover</Link><article className="destination-detail">{item.imageUrl&&<img src={item.imageUrl} alt={item.name}/>}<div><p className="eyebrow">Destination guide</p><h1>{item.name}</h1><p className="destination-location"><MapPinned size={16}/>{[item.city,item.state,item.country].filter(Boolean).join(", ")}</p><p>{item.description}</p><div className="destination-insights"><section><Sparkles size={18}/><b>Trip guide</b><p>{item.travelGuide||"Build your itinerary around local highlights and travel times."}</p></section><section><CloudSun size={18}/><b>{item.weather?.summary||"Weather"}</b><p>{item.weather?.temperatureRange||"Check the forecast before departure."}</p></section></div>{item.attractions&&<section className="guide-section"><h2>Popular attractions</h2><p>{item.attractions}</p></section>}<div className="detail-actions"><Link className="primary-button" to="/trips">Plan a trip here</Link><a className="secondary-button" href={item.mapUrl} target="_blank" rel="noreferrer">Open map</a></div></div></article></main>}
